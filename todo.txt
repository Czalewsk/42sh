insert char (t_read *, t_buf *, t_key *)
 -> insert un char ou wchar et incremente total_char && size_actual
 -> appel handler display si cest une insertion ailleurs qu'en fin de commande
 -> met a jour curs_char et appel handler display cursor

handler position cursor (t_read *)
 -> Fonction qui update le curseur selon la structure de controle
 -> Ce sont les fonctions qui incremente ou decremente curs_char mais
 -> handler position cursor qui le met a jour
	/!\ Colonne interdite /!\
